#!/usr/bin/env bash
#nginx installation
sudo apt-get -y update
sudo apt-get -y install nginx
sudo systemctl start nginx
sudo sed -i '63i add_header X-Served-By $hostname;' /etc/nginx/nginx.conf
sudo systemctl restart nginx
